kokoro>=0.9.2
soundfile
torch
sounddevice
numpy
loguru  # Modern logging with colors and formatting
python-dotenv>=1.0.0  # Load environment variables from .env file
google-cloud-aiplatform  # For Vertex AI / Gemini integration (optional)
langchain>=0.3.0              # Core LangChain framework (agents, chains)
langchain-core>=0.3.0         # LangChain core primitives (messages, tools)
langchain-google-genai>=2.0.0  # For Gemini AI Studio API key authentication
langchain-google-vertexai>=2.0.0  # For Vertex AI integration
langchain-community>=0.3.0    # Community integrations (ChatZhipuAI for GLM)
langchain-openai>=0.0.5       # For OpenAI-compatible APIs (Z.AI, OpenAI, etc.)
httpx_sse>=0.4.0            # Server-sent events support for streaming (Z.AI, etc.)
zhipuai>=2.1.0                # ZhipuAI Python SDK (required by ChatZhipuAI)
customtkinter>=5.2.0  # Modern GUI framework
Pillow>=10.0.0  # Image processing for GUI

# FastAPI backend for web app
fastapi>=0.115.0  # Web framework (compatible with pydantic 2.9+)
uvicorn[standard]>=0.32.0  # ASGI server
python-multipart>=0.0.6  # Form data parsing

# Development tools
ruff>=0.7.0  # Modern Python linter and formatter
pre-commit>=3.8.0
pyright>=1.1.380

# Scheduler packages
croniter>=6.0.0
nest-asyncio>=1.6.0
psycopg[binary,pool]>=3.3.3
dateparser>=1.2.0  # Natural language date/time parsing for schedules

# Browser automation (for Browser MCP tools)
playwright>=1.40.0       # Headless Chromium/Firefox/Webkit automation
